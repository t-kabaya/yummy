・シェアした際に、ホーム画面へと遷移する。
・postの中で、userInfoだけがネストしているのが気になる。
・userの代わりに、userNameを下手書きしても良いかもしれない。

・react-native-animatableのインストールが失敗する。

userIconを、至る所に適用する。（FeedScreen, commentScreen）

[無理] 投稿の仕方instaと同じにする。

staging環境を作成する
アイコンが、存在していない時、フィード画面のアイコンが、人型のものになっていない。